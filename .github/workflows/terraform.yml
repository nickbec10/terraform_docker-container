on:
  workflow_dispatch: {}

jobs: 
  terraform:
    runs-on: docker
    steps:
      - uses: actions/checkout@v3
      - uses: hashicorp/setup-terraform@v2
      - run: terraform init
      - run: terraform apply -auto-approve